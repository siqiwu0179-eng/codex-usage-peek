# Codex 周用量桌面插件产品需求文档（PRD）

| 项目 | 内容 |
|---|---|
| 产品暂定名 | Codex Usage Peek / Codex 周用量 |
| 文档版本 | v0.1 |
| 文档日期 | 2026-08-15 |
| 目标平台 | macOS（MacBook 优先） |
| 交付形态 | 原生 macOS 菜单栏/后台应用 + 桌面边缘悬浮 Card + WidgetKit 小组件 |
| 当前状态 | 需求定义；开发前必须先完成额度数据源可行性验证 |

## 1. 产品概述

为经常使用 Codex 的 macOS 用户提供一个轻量、低打扰的周用量查看工具。工具平时隐藏在屏幕边缘；鼠标移动到指定边缘时，Card 滑出并显示 Codex 当前一周剩余用量、重置日期；鼠标移开后自动收回。同时提供可添加到 macOS 桌面或通知中心的系统小组件。

本产品不负责调用 Codex 完成任务，也不改变用户的 Codex 套餐或额度，只读取和展示账户当前可用的用量信息。

## 2. 关键结论与开发前置条件

### 2.1 截图所表达的数据

参考截图显示：

- 指标名称：剩余用量
- 统计周期：1 周
- 剩余额度：78%
- 重置日期：8 月 20 日

产品必须将“剩余百分比”和“重置时间”视为服务端数据，不能通过本地 token 数自行臆测或用固定动画数值代替。

### 2.2 当前数据接口风险

截至本文撰写时，OpenAI 官方公开的 Usage API 文档描述的是 API Organization 的请求、token 与费用用量，并未公开说明它能够返回截图中 ChatGPT/Codex 订阅账户的“周剩余百分比”。因此：

- 不得把 OpenAI API Usage 数据冒充为 Codex 订阅周额度。
- 不得在 PRD 或 UI 中承诺已经可以通过普通 OpenAI API Key 获取该 78%。
- 开发第 0 阶段必须验证是否存在官方支持的 Codex 订阅额度接口、只读本地接口或受支持的账户授权方式。
- 若没有稳定、合规的数据源，产品只能以“技术预览”交付，启用手动更新或用户主动导入的降级模式，不能声称自动实时同步。

官方参考：[OpenAI Organization Usage API](https://developers.openai.com/api/reference/resources/admin/subresources/organization/subresources/usage)

### 2.3 数据接入优先级

按以下顺序实施，低优先级方案不得覆盖高优先级方案：

1. **官方订阅用量接口（首选）**：若 OpenAI 提供面向 Codex/ChatGPT 账户的官方接口或 SDK，采用官方账户授权并只申请读取用量所需的最小权限。
2. **官方或明确支持的本地只读接口**：若 Codex macOS App/CLI 提供公开命令、事件或 IPC，可由本地 Companion 读取规范化用量数据。
3. **用户主动导入**：通过“更新数据”操作粘贴数值、选择导出文件或运行明确展示给用户的本地命令。
4. **不纳入正式版**：抓取私有网页接口、读取 Codex/ChatGPT 登录 cookie、复制认证 token、绕过证书校验、持续 OCR 屏幕或模拟点击账户页面。这些方案安全性和稳定性不足。

## 3. 产品目标

### 3.1 核心目标

- 用户在不切换到 Codex App 的情况下，1 秒内看到本周剩余额度。
- 主应用在稳定数据源允许时自动刷新，并清楚展示数据新鲜度。
- Card 平时只保留极窄的边缘触发条，尽可能不遮挡工作内容。
- 同一份额度数据可同步显示在边缘 Card 和 macOS 小组件中。
- 不采集 prompt、代码、任务内容或工作区文件。

### 3.2 非目标

- 不预测 OpenAI 会如何计算额度。
- 不根据 API token 使用量反推订阅额度。
- 不提供多账户代登录、额度交易或共享账户功能。
- 首版不支持 Windows、iOS 或网页端。
- 首版不进行云端跨设备同步；所有快照默认仅存本机。

## 4. 目标用户与使用场景

### 4.1 目标用户

- 在 MacBook 上高频使用 Codex、需要控制每周任务节奏的个人用户。
- 希望看到重置日期、避免在关键任务前意外耗尽额度的用户。

### 4.2 核心场景

1. 用户工作时把鼠标移到屏幕右侧中部，Card 从边缘滑出，立即看到“剩余 78%，8 月 20 日重置”。
2. 用户将鼠标移出 Card，延迟短暂时间后 Card 自动收回，只留下 3–6 px 的触发区域或可配置的小把手。
3. 用户在桌面小组件中查看最近一次成功同步的周额度。
4. 自动更新失败时，Card 保留最后一次有效值，并明确显示“数据可能已过期”，而不是显示 0%。
5. 用户点击 Card 的刷新按钮，触发一次立即同步；若失败，可查看简短、可操作的错误原因。

## 5. 产品范围与版本规划

### 5.1 Phase 0：数据源可行性验证（Go / No-Go Gate）

必须先完成，暂不投入完整 UI 开发。

- 确认数据源是否能返回：`remainingPercent`、`period`、`resetAt`、`fetchedAt`。
- 验证登录过期、网络断开、额度重置和多账户切换场景。
- 连续运行至少 7 天，确认读数与 Codex App 中显示的值一致。
- 记录数据来源、授权方式、刷新限制、服务条款风险和失败码。
- 禁止把用户的会话 cookie 或 token 写入日志。

**Go 条件**：存在稳定且用户明确授权的只读数据源，连续对照误差为 0 个百分点，重置时间一致。

**No-Go 条件**：只能依赖私有接口、凭据提取、持续屏幕录制，或无法稳定获得周剩余百分比。此时产品转为手动更新原型。

### 5.2 MVP

- 单账户、单一周用量指标。
- 右侧边缘触发，支持改为左侧。
- Card 显示剩余百分比、进度条、重置日期、最后更新时间、同步状态。
- 自动隐藏、鼠标悬停保持、点击固定/取消固定。
- 手动刷新。
- macOS 小/中尺寸小组件。
- 登录时启动开关。
- 浅色/深色模式和 Reduce Motion 适配。
- 本地缓存最后一次有效数据。

### 5.3 后续版本

- 上/下/左/右边缘及垂直位置拖动。
- 多显示器分别配置。
- 5 小时与 1 周等多个周期（前提是数据源真实提供）。
- 低额度通知阈值，例如 20%、10%、5%。
- 菜单栏紧凑视图、历史趋势和多账户切换。

## 6. 功能需求

### FR-01 用量数据读取

- 系统通过选定的数据 Provider 获取账户用量。
- Provider 必须返回统一数据模型，不允许 UI 直接解析网页或内部响应。
- 首次安装且尚无数据时显示“尚未连接用量数据”，不得显示虚构的 76% 或 78%。
- 成功刷新后同时更新主 Card、本地缓存和 Widget 共享数据。

### FR-02 更新策略

- 主应用运行时：默认每 60 秒检查一次是否需要刷新；实际网络请求频率必须遵守数据源限制，建议最短 5 分钟一次。
- 用户点击刷新：允许立即请求，但 30 秒内重复点击应合并。
- 若可获得 Codex 本地任务完成事件：任务结束后延迟 5–15 秒触发一次刷新。
- 失败重试：指数退避，建议 1、5、15、30 分钟；成功后恢复正常周期。
- 断网时展示最后成功数据、最后更新时间与离线标识。
- “实时”定义为数据源允许条件下的分钟级或事件触发同步，不承诺秒级推送。

### FR-03 边缘 Card

- 默认位置：主显示器右侧垂直居中。
- 收起态：保留 3–6 px 透明/半透明触发条；可选显示 20–36 px 小把手。
- 鼠标进入触发条后 100–200 ms 内开始展开。
- 展开动画时长 220–320 ms，使用 ease-out 或 spring，避免原 CSS 中首次加载 0.8 秒带来的迟滞。
- 鼠标进入 Card 后保持展开。
- 鼠标离开 Card 500 ms 后收起；若鼠标在延迟内返回，取消收起。
- 支持固定展开；按 `Esc` 或点击取消固定后恢复自动隐藏。
- Card 不抢夺键盘焦点，除非用户点击可交互控件。
- 默认仅在普通 Space 显示；“所有桌面/全屏应用中显示”为可选设置，避免打扰演示和视频。

### FR-04 Card 内容

由上到下：

1. 状态图标与标题“Codex 剩余用量”。
2. 主数值，例如 `78%`。
3. 语义文本，例如“本周剩余”。
4. 动态进度条，宽度必须绑定真实 `remainingPercent`。
5. 重置信息，例如“预计 8 月 20 日重置”；有精确时间时支持 Tooltip 展示时区。
6. 最后更新时间，例如“刚刚更新”或“15 分钟前”。
7. 同步错误时显示简短状态及“重试”操作。

### FR-05 系统小组件

- 使用 WidgetKit 提供 `systemSmall` 与 `systemMedium` 两种尺寸。
- 小组件读取 App Group 中的最后一次有效快照，不直接保存登录凭据。
- 主应用成功同步后调用 WidgetKit 刷新请求；具体刷新时间由 macOS 管理。
- 小组件必须显示最后更新时间；超过 30 分钟显示“可能已过期”，超过 24 小时显示“打开应用刷新”。阈值后续可配置。
- 点击小组件打开主应用并展开 Card 或进入状态页。
- 小组件不提供鼠标靠边展开逻辑；它是系统管理的静态/时间线视图。

Apple 参考：[Keeping a widget up to date](https://developer.apple.com/documentation/widgetkit/keeping-a-widget-up-to-date)、[WidgetCenter reloadAllTimelines](https://developer.apple.com/documentation/widgetkit/widgetcenter/reloadalltimelines%28%29)

### FR-06 设置

- 边缘：左 / 右（MVP）。
- 垂直位置：上 / 中 / 下（MVP 可只提供中，后续支持拖动）。
- 自动隐藏延迟：立即 / 0.5 秒 / 1 秒 / 2 秒。
- 登录时启动。
- 在所有桌面显示。
- Reduce Motion 跟随系统。
- 手动断开数据源并清除本地凭据。

### FR-07 状态与错误展示

| 状态 | Card 行为 | 小组件行为 |
|---|---|---|
| 正常 | 显示最新百分比、绿色进度与更新时间 | 显示最新快照 |
| 首次未连接 | 显示连接说明，不显示百分比 | 显示“请打开应用连接” |
| 离线 | 保留最后有效值，显示离线 | 保留快照并标记过期 |
| 登录失效 | 保留最后有效值，提示重新授权 | 显示“需要重新连接” |
| 数据格式变化 | 不覆盖缓存，记录本地诊断 | 继续显示旧值并标记过期 |
| 用量耗尽 | 显示 0%，使用警示色 | 显示 0% 与重置日期 |
| 数据源无此字段 | 显示“不支持周用量” | 不展示伪造数值 |

## 7. 交互流程

### 7.1 首次启动

1. 欢迎页说明产品只读取用量，不读取对话、代码或项目文件。
2. 用户选择可用的数据连接方式。
3. 产品展示将申请的权限及用途。
4. 连接成功后拉取首个快照，并引导用户把系统小组件添加到桌面。
5. 展示边缘触发条位置，用户完成一次鼠标移入/移出演练。

### 7.2 日常查看

1. 用户将鼠标移至指定边缘。
2. 触发条命中后 Card 滑入。
3. Card 立即显示缓存数据；若需要刷新，在后台刷新并平滑更新数值。
4. 鼠标移开，Card 在设定延迟后滑回。

### 7.3 更新失败

1. 请求失败时不清空最后有效快照。
2. Card 将更新时间变为橙色并显示错误分类。
3. 用户可点击重试或重新连接。
4. 诊断信息仅包含时间、Provider、错误码和应用版本，不包含凭据。

## 8. UI 与视觉规范

### 8.1 原始代码的保留元素

- 白色到浅灰的柔和渐变背景。
- 28 px 大圆角和轻量阴影。
- 绿色状态图标、绿色渐变进度条。
- SF Pro 系统字体、左对齐的大百分比。
- 轻微呼吸/进度动画。

### 8.2 必要修改

- CSS 仅作为视觉参考；原生 macOS 版本使用 SwiftUI/AppKit 重写，不在原生窗口内套 WebView。
- `.fill` 宽度不得写死为 76%，必须绑定后端返回值。
- 原代码的 `cardFadeUp` 改为从屏幕边缘横向滑入，而非纵向上浮。
- 首次 0.8 秒动画缩短到 0.22–0.32 秒，符合“随用随看”。
- `card:hover` 不再上移 6 px，避免 Card 在贴边状态下产生位置跳动；悬停只增强阴影或描边。
- 增加深色模式、系统强调色、Reduce Motion 和高对比度适配。
- 根据截图补充“1 周”“重置日期”和折叠/展开语义，不只显示单一百分比。

### 8.3 Card 建议规格

| 项目 | 建议值 |
|---|---|
| 展开宽度 | 300–340 px |
| 内边距 | 20–24 px |
| 圆角 | 24–28 px；靠屏幕边缘一侧可减小 |
| 主数值 | 38–44 pt，Semibold/Bold |
| 标题 | 17–19 pt，Semibold |
| 辅助文字 | 12–14 pt |
| 进度条高度 | 8–10 px |
| 展开动画 | 220–320 ms |
| 自动隐藏延迟 | 默认 500 ms |

### 8.4 色彩语义

- 51–100%：绿色。
- 21–50%：黄色/琥珀色。
- 1–20%：橙红色。
- 0%：红色，并突出重置时间。
- 颜色不能作为唯一信息载体，必须同时显示数值和文本。

## 9. 技术方案建议

### 9.1 技术栈

- Swift + SwiftUI：界面、设置页和小组件。
- AppKit `NSPanel`：无标题、非激活式悬浮 Card 和边缘触发窗口。
- WidgetKit：macOS 桌面/通知中心小组件。
- App Group：主应用与 Widget Extension 共享只读快照。
- Keychain：保存官方授权凭据；Widget 不得访问或复制原始凭据。
- `SMAppService`：用户开启“登录时启动”后注册登录项。

Apple 参考：[NSPanel](https://developer.apple.com/documentation/appkit/nspanel)、[SMAppService](https://developer.apple.com/documentation/servicemanagement/smappservice)

### 9.2 逻辑结构

```text
数据源 Provider
      ↓
Usage Sync Service ──→ Keychain（仅凭据）
      ↓
UsageSnapshot Store（App Group）
      ├──→ Edge Card / 设置页
      └──→ WidgetKit Timeline
```

### 9.3 统一数据模型

```swift
struct UsageSnapshot: Codable {
    let remainingPercent: Double   // 0...100
    let period: UsagePeriod        // weekly
    let resetAt: Date?
    let fetchedAt: Date
    let source: UsageSource
    let accountLabel: String?
    let status: UsageStatus
}
```

规则：

- 对 `remainingPercent` 做 0–100 范围校验；异常响应不得覆盖旧快照。
- `resetAt` 以绝对时间保存，UI 按当前系统时区显示。
- 对账户标识做脱敏，例如只显示邮箱前缀或自定义昵称。
- 使用原子写入，防止 Widget 读取到半写入文件。

### 9.4 边缘命中方案

- 创建 3–6 px 宽的透明 AppKit 窗口作为应用自有触发区。
- 通过窗口自身的 mouse-enter/mouse-exit tracking 触发展开，不监听全局键盘输入。
- 该方案原则上不需要“辅助功能”或“屏幕录制”权限。
- 多显示器时为用户指定显示器创建触发区；显示器拔插后重新计算安全区域。
- 需避开 Dock、刘海、菜单栏和 Stage Manager 可能占用区域。

## 10. 资源与权限需求

### 10.1 用户需要提供或确认的资源

| 资源 | 必需性 | 用途 |
|---|---|---|
| 一台安装 Codex 的 MacBook | 必需 | 开发与真实环境验证 |
| 可显示截图中“剩余用量”的 Codex 账户 | 必需 | Phase 0 数据一致性验证 |
| 允许查看用量页/账户面板 | 必需 | 只用于比对读数，不应读取对话内容 |
| Xcode 与可用 Apple ID Team | 必需 | 编译主 App 和 Widget Extension |
| Apple Developer Program 会员 | 本机自用可暂缓；对外分发必需 | Developer ID 签名、公证和稳定分发 |
| App Group 标识 | 必需 | 主 App 与 Widget 共享快照 |
| OpenAI 官方授权/接口凭据 | 条件必需 | 仅当官方提供可读取订阅用量的方式时使用 |
| 最终产品名、图标和安装方式 | 后续必需 | 精细化 UI、签名与发布 |

### 10.2 macOS 权限与 Entitlements

| 权限/能力 | MVP 是否需要 | 说明 |
|---|---|---|
| Outgoing Network Connections | 条件需要 | 调用官方用量数据源 |
| App Groups | 需要 | 主 App 与小组件共享快照 |
| Keychain Access | 条件需要 | 保存官方授权凭据 |
| Login Item | 用户可选 | 登录时自动运行，由用户在设置中开启 |
| Notifications | 后续可选 | 低额度提醒；首次触发前向用户解释用途 |
| Accessibility | 默认不需要 | 只使用应用自有边缘透明窗口时无需申请 |
| Screen Recording | 默认不需要 | 正式版不持续读取或 OCR 其他应用画面 |
| Full Disk Access | 不需要 | 产品不得扫描用户文件或 Codex 工作区 |
| Apple Events/Automation | 不需要 | 正式版不通过 UI 脚本控制 Codex |

若最终数据方案必须新增辅助功能、屏幕录制或自动化权限，必须回到产品评审，向用户明确解释原因；不得静默扩大权限范围。

### 10.3 开发阶段需要用户配合的最少事项

1. 在真实 Codex 账户中确认截图数值，并允许开发版进行只读对照测试。
2. 如出现官方授权页，由用户本人完成登录；用户无需把密码发给开发者或写进配置文件。
3. 在 Xcode 中选择自己的 Team；若需要正式安装包，再提供 Developer ID/公证所需的 Apple 开发者资格。
4. 把开发版小组件添加到桌面，验证其显示和刷新行为。
5. 最终确认 Card 位于左侧还是右侧、是否覆盖全屏应用、自动隐藏延迟。

## 11. 安全与隐私要求

- 数据最小化：只存储剩余百分比、周期、重置时间、更新时间、数据源和脱敏账户标签。
- 不收集用户 prompt、回答、代码、文件路径、任务名称或浏览历史。
- 凭据仅存 Keychain；日志、UserDefaults、App Group 文件中不得出现明文 token/cookie。
- Widget 只读取快照，不直接发起需要账户凭据的请求。
- 提供“断开并清除数据”功能，清除凭据和本地缓存前进行确认。
- 默认关闭远程遥测；如未来加入崩溃上报，必须单独说明并提供关闭选项。
- 所有错误日志均需脱敏，可由用户预览后再导出。

## 12. 非功能需求

- 冷启动后 1 秒内可用缓存渲染 Card。
- 待机内存目标小于 80 MB，空闲 CPU 平均小于 1%。
- 不因轮询导致明显电量消耗；后台刷新应合并并采用退避策略。
- 支持当前开发基线及其前两个主要 macOS 版本，具体最低版本在开发启动时确认。
- 屏幕分辨率、缩放、多显示器变化后不出现 Card 永久移出屏幕。
- 网络和数据源故障不得导致应用崩溃或把真实剩余量误写为 0%。
- VoiceOver 能朗读“本周剩余 78%，预计 8 月 20 日重置”。

## 13. 验收标准

### 13.1 数据准确性

- 连续 7 天随机抽查至少 20 次，插件数值与 Codex 官方界面一致。
- 重置发生后，下一次成功同步能正确更新百分比与新的重置时间。
- 离线、登录失效或响应结构变化时，保留最后有效值并标记过期。

### 13.2 边缘交互

- 鼠标进入设定边缘后，95% 的测试在 200 ms 内开始展开。
- 鼠标停留于 Card 上时不自动隐藏。
- 鼠标移开后按设定延迟收回，快速返回不会闪烁。
- 切换显示器、Space、缩放和 Dock 位置后 Card 仍可被找回。

### 13.3 小组件

- 用户可以在 macOS 小组件库中找到并添加该组件。
- 主应用同步成功后，App Group 快照立即更新，并向系统请求刷新 Widget 时间线。
- 即使 macOS 延迟刷新，小组件仍显示可理解的最后更新时间，不伪装成实时数据。
- 主应用被退出后，小组件仍能显示最后有效快照。

### 13.4 权限与隐私

- 未授予屏幕录制、辅助功能和完全磁盘访问时，MVP 核心边缘交互仍可运行。
- 断开账户后 Keychain 中的相关凭据被删除。
- 导出的诊断日志不包含 token、cookie、prompt 或文件内容。

## 14. 主要风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 无官方订阅额度接口 | 无法自动显示真实百分比 | Phase 0 先验证；保留手动模式；不伪造数据 |
| 私有接口随时变化 | 数据中断、账户安全风险 | 不作为正式版依赖；Provider 隔离；版本化解析 |
| WidgetKit 刷新受系统调度 | 小组件不是秒级实时 | 显示最后更新时间；主 Card 承担高频查看 |
| 透明边缘窗口干扰点击 | 影响用户工作 | 触发区保持 3–6 px；位置可调；提供暂停开关 |
| 多显示器/全屏 Space 行为复杂 | Card 丢失或打扰 | MVP 默认主屏；提供“重置位置”；逐场景验收 |
| 登录凭据泄露 | 高安全风险 | 官方授权优先、Keychain、最小权限、日志脱敏 |
| OpenAI 产品 UI/限额规则变化 | 字段和语义漂移 | 服务端字段校验、状态提示、可更新 Provider |

## 15. 开发里程碑

| 阶段 | 交付物 | 完成条件 |
|---|---|---|
| M0 数据 Spike | 数据源验证报告、样例数据、权限清单 | 通过 Go 条件或明确转手动模式 |
| M1 交互原型 | 假数据 Edge Card、设置页 | 展开/收起、多 Space 基础验证通过 |
| M2 数据接入 | Provider、缓存、错误状态 | 与官方界面对照一致 |
| M3 Widget | 小/中尺寸组件、App Group 同步 | 可添加且过期状态正确 |
| M4 精细化 | 依据用户后续代码调整 UI | 浅色/深色/动效/无障碍验收通过 |
| M5 打包 | 签名安装包、隐私说明、使用说明 | 新机器可安装并完成首次连接 |

## 16. 待用户确认项

这些问题不阻塞 PRD，但应在 M1 前确认：

1. Card 默认贴左侧还是右侧；是否只在主屏显示。
2. 是否希望 Card 覆盖全屏 App，还是只在桌面和普通窗口 Space 出现。
3. 是否需要同时显示其他周期，例如 5 小时额度；数据源必须真实提供后才能加入。
4. 低额度提醒阈值及是否需要系统通知。
5. 仅个人本机安装，还是希望制作可分享的签名 `.dmg`。
6. 产品正式名称、图标风格，以及后续用于精细化的 UI 代码/参考图。

## 17. 产品成功指标

- 数据准确率：与官方 Codex 界面对照 100% 一致。
- 查看效率：从鼠标触边到读到数据不超过 1 秒。
- 稳定性：7 天持续运行无崩溃、无 Card 丢失。
- 资源占用：空闲 CPU 平均低于 1%，无明显电量影响。
- 隐私：不申请非必要的屏幕录制、辅助功能或完全磁盘访问权限。

---

本 PRD 的最重要研发原则是：**先证明能够合规、稳定地读取截图中的 Codex 周剩余额度，再开发完整 UI；任何估算值都不能冒充账户真实额度。**
